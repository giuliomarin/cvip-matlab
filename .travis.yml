language: python

os:
  - linux

python:
  - "2.7"

before_install:
  - sudo apt-get update
  - sudo apt-get install python-opencv
  - sudo dpkg -L python-opencv
  - export PYTHONPATH=/usr/lib/pyshared/python2.7:/usr/local/lib/python2.7/dist-packages:$PYTHONPATH
  - sudo pip install matplotlib
  - sudo pip install numpy
  - echo $PATH
  - echo $PYTHONPATH

script:
  - python python/unittests/main.py

notifications:
  email: false
